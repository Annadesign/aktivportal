
<div class="ibox">
  <div class="ibox-content">
    <div class="panel-body">  
      <p id="notice"><%= notice %></p>


      <table class="table table-bordered table-hover" >
        <thead>
          <tr>
            <th>Tilbydere</th>
          
           
            <th colspan="2">Handlinger</th>
          </tr>
        </thead>

        <tbody>
          <% @providers.each do |cat| %>
            <tr>
              <td><%= cat.title %></td>
          
         
              <td><%= link_to 'Redigere', edit_admin_provider_path(cat) %></td>
              <td><%= link_to 'Slett', admin_provider_path(cat), method: :delete, data: { confirm: 'Er du sikker?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'Ny kategori', new_admin_provider_path %>

    </div>
  </div>
</div>
